mode: Mainline
branches:
  master:
    regex: ^master$
    label: ''
    increment: Patch
    is-release-branch: true
  develop:
    regex: ^dev(elop)?(ment)?$
    label: 'alpha'
    increment: Minor
  feature:
    regex: ^features?[/-]
    label: 'feature'
    increment: Minor
ignore:
  sha: []
